{{!-- Layout for the home page --}}
{{> header}}

<main>
  {{#each posts}}
    <article>
      <h2>{{this.title}}</h2>
      <p>{{this.content}}</p>
      <footer>
        <p>Posted by {{this.user.username}} on {{this.createdAt}}</p>
      </footer>
    </article>
  {{/each}}
</main>

{{> footer}}