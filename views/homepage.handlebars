<main>
  {{#each posts}}
    <article>
      <h2>{{this.title}}</h2>
      <p>{{this.content}}</p>
      <footer>
        <p>Posted by {{this.user.username}} on {{format_date this.createdAt}}</p>
      </footer>
    </article>
  {{/each}}
</main>